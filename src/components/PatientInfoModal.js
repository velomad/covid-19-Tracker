import React from "react";
import Modal from 'react-modal';

const PatientInfoModal = () => {
  return (
    <div className="PatientInfoModal">
     test text
    </div>
  );
};

export default PatientInfoModal;
